---
phase: 12-stability-gates-regression-reliability
plan: 01
type: execute
wave: 1
depends_on: [Phase 11]
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

# Phase 12 Plan 01: Stability Gates & Regression Reliability

## Objective

- **What:** 添加稳定性门禁和回归测试，确保 polyglot 扩展后质量可信
- **Why:** 防止版本漂移、接线错误和性能退化
- **Output:** 跨语言集成测试、scanner/registry 一致性检查、回归基线

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stability-gates-regression-reliability/12-CONTEXT.md

## Tasks

<task type="code">
  <name>Add polyglot integration test</name>
  <files>crates/wtcd-tests/tests/integration_polyglot.rs</files>
  <action>Add integration tests that verify wtcd run works with all 9 new languages, checking parse results include exports/imports/signatures with correct confidence. Verify all extensions are routed to correct adapters.</action>
  <verify>New tests pass</verify>
  <done>Integration tests cover all 13 languages</done>
</task>

<task type="code">
  <name>Add scanner-registry consistency test</name>
  <files>crates/wtcd-tests/tests/</files>
  <action>Add test that verifies every file extension registered in adapter registry is recognized by the scanner, and vice versa. Catches mismatch before merge.</action>
  <verify>Consistency test passes</verify>
  <done>Scanner-registry mismatch test exists and passes</done>
</task>

<task type="code">
  <name>Add regression test for existing adapters</name>
  <files>crates/wtcd-tests/tests/</files>
  <action>Add test that runs wtcd run on existing TS/JS/Python/Go fixtures and verifies no regression in output format or parse quality.</action>
  <verify>Regression tests pass</verify>
  <done>Existing adapter regression tests pass</done>
</task>

<task type="code">
  <name>Add parse performance budget test</name>
  <files>crates/wtcd-tests/tests/</files>
  <action>Add test that verifies parse_time_ms is under 100ms for all fixture files across all 13 languages. Fails if any file exceeds threshold.</action>
  <verify>Performance test passes</verify>
  <done>Parse time budget test exists and passes</done>
</task>

## Verification

1. Run `cargo test` — all tests pass including new stability tests
2. Verify scanner-registry consistency test catches mismatches
3. Verify parse_time_ms budget test passes for all languages

## Success Criteria

- [ ] ADPT-12: Fixtures + unit tests + integration tests for all new adapters
- [ ] ROUT-04: Regression checks on representative repositories
- [ ] STAB-01: CI matrix validation across all languages
- [ ] STAB-02: Performance budgets with measurable thresholds
- [ ] STAB-03: Parser/grammar version locks documented
- [ ] STAB-04: Scanner-extension and adapter-registry mismatch test
