---
phase: "999.1-mcp-mcp-cli-agent"
plan: 3
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - docs/12_MCP_Server_Contract.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "MCP Server 端到端可运行：`wtcd mcp` 启动后响应 initialize/list_tools/call_tool"
    - "wtcd_parse 工具可以解析项目中的 TS/JS 文件并返回 JSON 结构"
    - "wtcd_run 工具可以扫描整个项目并返回分析结果"
    - "MCP Server 规范文档存在且描述了工具契约"
  artifacts:
    - path: "docs/12_MCP_Server_Contract.md"
      provides: "MCP Server 工具契约规范（ADR 流程）"
  key_links:
    - from: "crates/wtcd-mcp/src/server.rs"
      to: "Agent (Claude Code)"
      via: "stdio JSON-RPC"
      pattern: "stdio\\(\\)"
---

<objective>
端到端验证 MCP Server + 编写 MCP 规范文档

**Purpose:** 确认 MCP Server 与 Agent 的完整集成链路可以工作，并产出规范文档供后续开发参考

**Output:** 端到端验证通过 + docs/12_MCP_Server_Contract.md 规范文档
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-CONTEXT.md
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-RESEARCH.md
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-01-SUMMARY.md
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-02-SUMMARY.md
@docs/04_Architecture_and_Component_Model.md
@docs/08_Agent_Runtime_Contract_and_Workflows.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: End-to-end MCP Server verification</name>
  <files>(verification only — no files created)</files>
  <read_first>crates/wtcd-mcp/src/server.rs, crates/wtcd-mcp/src/tools/parse.rs, crates/wtcd-mcp/src/tools/run.rs</read_first>
  <action>
    Run the following verification steps:

    1. **Build check:** `cargo build --workspace` must pass

    2. **MCP initialize test:** Write a JSON-RPC initialize request to wtcd mcp's stdin and verify response:
       ```bash
       echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"test","version":"0.1"}}}' | timeout 5 cargo run -p wtcd-cli -- mcp 2>/dev/null
       ```
       Expected: Response contains `"serverInfo":{"name":"wtcd-mcp"}`

    3. **List tools test:** Send tools/list after initialize:
       ```bash
       printf '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"test","version":"0.1"}}}\n{"jsonrpc":"2.0","id":2,"method":"notifications/initialized"}\n{"jsonrpc":"2.0","id":3,"method":"tools/list"}\n' | timeout 5 cargo run -p wtcd-cli -- mcp 2>/dev/null
       ```
       Expected: Response contains all 4 tools: wtcd_parse, wtcd_run, wtcd_check, wtcd_mirror

    4. **wtcd_parse test:** Call wtcd_parse on a known file:
       ```bash
       printf '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"test","version":"0.1"}}}\n{"jsonrpc":"2.0","id":2,"method":"notifications/initialized"}\n{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"wtcd_parse","arguments":{"file_path":"crates/wtcd-core/src/lib.rs"}}}\n' | timeout 10 cargo run -p wtcd-cli -- mcp 2>/dev/null
       ```
       Expected: Response contains parse result (exports, imports, etc.)

    5. **Verify stderr logging:** Ensure logs go to stderr, not stdout

    If any test fails, document the failure in the SUMMARY.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>All verification steps pass. MCP Server responds correctly to initialize, tools/list, and tools/call requests via stdio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1b: Manual MCP integration verification (human checkpoint)</name>
  <files>(none — manual verification)</files>
  <read_first>crates/wtcd-mcp/src/server.rs, crates/wtcd-cli/src/commands/mcp_install.rs</read_first>
  <action>
    **PAUSE FOR HUMAN VERIFICATION — MCP Server + Claude Code Integration**

    The executor must wait for human confirmation before proceeding to Task 2.

    Instructions for the human verifier:
    1. Run `cargo run -p wtcd-cli -- mcp-install claude` and copy the output
    2. Add the config to ~/.claude.json (merge with existing mcpServers if present)
    3. Open a new Claude Code session in a project with anrsm.yaml
    4. Check that the wtcd MCP tools appear in Claude Code's tool list
    5. Ask Claude Code to use wtcd_parse on a file — verify it returns structured output
    6. Ask Claude Code to use wtcd_run — verify project scan works end-to-end
    7. Report results: "approved" or describe issues found
  </action>
  <verify>
    <automated>echo "HUMAN_CHECKPOINT — await user confirmation before proceeding"</automated>
  </verify>
  <done>Human verifier has confirmed MCP Server integration works with Claude Code (or issues have been documented)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write MCP Server Contract specification document</name>
  <files>docs/12_MCP_Server_Contract.md</files>
  <read_first>docs/04_Architecture_and_Component_Model.md, docs/08_Agent_Runtime_Contract_and_Workflows.md, .planning/phases/999.1-mcp-mcp-cli-agent/999.1-CONTEXT.md</read_first>
  <action>
    Create `docs/12_MCP_Server_Contract.md` following the existing docs format (Normative, v1, P0):

    Structure:
    ```markdown
    # 12. MCP Server Contract

    状态：Normative
    版本：v1
    优先级：P0

    ---

    ## 1. 目的
    定义 WTCD 通过 MCP 协议暴露给 Agent 的工具契约、传输约束和服务生命周期。

    ## 2. 传输层
    - 仅支持 stdio 传输（D-03）
    - 日志输出 stderr，JSON-RPC 走 stdout
    - Agent 作为 parent process 管理 MCP Server 生命周期

    ## 3. 工具定义
    ### 3.1 wtcd_parse
    - 功能：解析单个文件，提取语义事实
    - 参数：{ file_path: string, language?: string }
    - 返回：FileResult JSON
    - 置信度：high/low/none

    ### 3.2 wtcd_run
    - 功能：扫描项目，执行全量语义分析
    - 参数：{ project_root?: string }
    - 返回：RunOutput JSON（含 files[] + summary）

    ### 3.3 wtcd_check (stub)
    - 功能：检测镜像漂移（Phase 3 实现）
    - 返回："not implemented" error

    ### 3.4 wtcd_mirror (stub)
    - 功能：生成镜像文件（Phase 2 实现）
    - 返回："not implemented" error

    ## 4. 配置发现
    - 首次工具调用时自动查找 anrsm.yaml
    - 支持 project_root 参数显式指定

    ## 5. Agent 集成
    - Claude Code: ~/.claude.json mcpServers 配置
    - Cursor: .cursor/mcp.json 配置
    - 通用: `wtcd mcp-install --agent <name>` 生成配置

    ## 6. 与 CLI 的关系
    - MCP 和 CLI 共享 wtcd-core/wtcd-adapters/wtcd-scope（D-01）
    - MCP 直接调用 Rust API，不走 CLI 子进程（D-02）
    - MCP 和 CLI 功能对等（D-01）
    ```

    Follow the normative language conventions from docs/02 (MUST, SHOULD, MAY).
    Reference decisions D-01 through D-14 from CONTEXT.md where applicable.
  </action>
  <verify>
    <automated>test -f docs/12_MCP_Server_Contract.md && echo "PASS: doc exists" || echo "FAIL: doc missing"</automated>
  </verify>
  <done>docs/12_MCP_Server_Contract.md 存在，包含 4 个工具定义、传输约束、配置发现和 Agent 集成说明</done>
</task>

</tasks>

<verification>
```bash
# Full workspace build
cargo check --workspace

# Doc exists
test -f docs/12_MCP_Server_Contract.md && echo "PASS"

# Final integration test
cargo run -p wtcd-cli -- mcp-install claude | python3 -m json.tool > /dev/null && echo "PASS"
```
</verification>

<success_criteria>
- [ ] cargo build --workspace 通过
- [ ] MCP Server 响应 initialize + tools/list + tools/call
- [ ] wtcd_parse 正确解析文件并返回 JSON
- [ ] wtcd_run 正确扫描项目并返回分析结果
- [ ] Claude Code 可以发现并使用 wtcd MCP 工具（checkpoint 验证）
- [ ] docs/12_MCP_Server_Contract.md 存在且完整
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-mcp-cli-agent/999.1-03-SUMMARY.md`
</output>
