---
phase: "999.1-mcp-mcp-cli-agent"
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - crates/wtcd-cli/Cargo.toml
  - crates/wtcd-cli/src/main.rs
  - crates/wtcd-cli/src/commands/mod.rs
  - crates/wtcd-cli/src/commands/mcp.rs
  - crates/wtcd-cli/src/commands/mcp_install.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "用户可以运行 `wtcd mcp` 启动 MCP Server（stdio 模式）"
    - "用户可以运行 `wtcd mcp-install --agent claude` 获取 Claude Code 配置片段"
    - "MCP Server 以 stderr 记录日志，stdout 留给 JSON-RPC"
    - "mcp-install 支持 claude 和 cursor 两种 agent"
  artifacts:
    - path: "crates/wtcd-cli/src/commands/mcp.rs"
      provides: "MCP Server stdio entry point via clap subcommand"
    - path: "crates/wtcd-cli/src/commands/mcp_install.rs"
      provides: "Agent 配置片段生成命令"
    - path: "crates/wtcd-cli/src/main.rs"
      provides: "CLI 入口添加 Mcp 和 McpInstall 子命令"
  key_links:
    - from: "crates/wtcd-cli/src/commands/mcp.rs"
      to: "wtcd-mcp::server::WtcdMcpServer"
      via: "创建 WtcdMcpServer 并调用 serve(stdio())"
      pattern: "WtcdMcpServer::new.*serve.*stdio"
---

<objective>
集成 MCP Server 到 CLI — 添加 `wtcd mcp` 和 `wtcd mcp-install` 子命令

**Purpose:** 让用户通过 `cargo install wtcd` 后直接运行 MCP Server，无需额外安装

**Output:** CLI 扩展支持 MCP 子命令，一键启动 MCP Server + 生成 Agent 配置
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-CONTEXT.md
@crates/wtcd-cli/src/main.rs
@crates/wtcd-cli/src/commands/mod.rs
@crates/wtcd-mcp/src/server.rs
@.planning/ROADMAP.md

<interfaces>
From wtcd-cli/src/main.rs (current CLI structure):
- `Cli` struct with clap::Parser
- `Commands` enum: `Init {}`, `Run {}`
- main() matches on Commands and dispatches

From wtcd-mcp/src/server.rs (created in Plan 01):
- `WtcdMcpServer::new(project_root: PathBuf) -> Self`
- `.serve(stdio()).await` — starts stdio transport

D-11: `wtcd mcp-install --agent <name>` — 生成 Agent 配置片段
D-12: 单一二进制含 MCP 模式（`wtcd mcp` 子命令）
D-13: MCP Server 生命周期 = 预加载 + 按需
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `wtcd mcp` subcommand — stdio MCP Server entry point</name>
  <files>crates/wtcd-cli/Cargo.toml, crates/wtcd-cli/src/main.rs, crates/wtcd-cli/src/commands/mod.rs, crates/wtcd-cli/src/commands/mcp.rs</files>
  <read_first>crates/wtcd-cli/src/main.rs, crates/wtcd-cli/src/commands/mod.rs, crates/wtcd-mcp/src/server.rs</read_first>
  <action>
    1. Add `wtcd-mcp` dependency to `crates/wtcd-cli/Cargo.toml`:
       ```toml
       wtcd-mcp = { workspace = true }
       ```
    2. Add `wtcd-mcp = { path = "crates/wtcd-mcp" }` to workspace.dependencies in root Cargo.toml

    3. Create `crates/wtcd-cli/src/commands/mcp.rs`:
       ```rust
       use std::path::PathBuf;
       use rmcp::{ServiceExt, transport::stdio};
       use wtcd_mcp::server::WtcdMcpServer;

       pub async fn run_mcp(root: &PathBuf) -> anyhow::Result<()> {
           tracing_subscriber::fmt()
               .with_writer(std::io::stderr)
               .with_env_filter(
                   tracing_subscriber::EnvFilter::try_from_default_env()
                       .unwrap_or_else(|_| tracing_subscriber::EnvFilter::new("info"))
               )
               .init();

           tracing::info!("Starting WTCD MCP Server (stdio)");
           let server = WtcdMcpServer::new(root.clone());
           let service = server.serve(stdio()).await.inspect_err(|e| {
               tracing::error!("MCP server error: {}", e);
           })?;
           tracing::info!("WTCD MCP Server connected");
           service.waiting().await?;
           Ok(())
       }
       ```

    4. Update `crates/wtcd-cli/src/commands/mod.rs`:
       - Add `pub mod mcp;`
       - Add `pub mod mcp_install;`

    5. Update `crates/wtcd-cli/src/main.rs`:
       - Add `Mcp {}` variant to Commands enum (with doc: "Start MCP Server (stdio mode)")
       - Add `McpInstall { agent: String }` variant (with doc: "Print MCP config for an agent [claude|cursor]")
       - In main(), change from `fn main()` to `#[tokio::main] async fn main()` (since MCP is async)
       - Add match arms:
         - `Commands::Mcp {} => commands::mcp::run_mcp(&cli.root).await`
         - `Commands::McpInstall { agent } => commands::mcp_install::install_for_agent(&agent)`
       - Note: existing Init/Run calls remain synchronous but inside async main (they don't need to be async themselves)
  </action>
  <verify>
    <automated>cargo build -p wtcd-cli 2>&1 | tail -10</automated>
  </verify>
  <done>`wtcd mcp --help` 显示 MCP Server 说明。`wtcd mcp` 启动后等待 stdio 输入。cargo build 成功。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `wtcd mcp-install` subcommand — agent config generator</name>
  <files>crates/wtcd-cli/src/commands/mcp_install.rs</files>
  <read_first>crates/wtcd-cli/src/main.rs, .planning/phases/999.1-mcp-mcp-cli-agent/999.1-CONTEXT.md</read_first>
  <action>
    Create `crates/wtcd-cli/src/commands/mcp_install.rs`:

    ```rust
    use anyhow::{anyhow, Result};

    /// Generate MCP config snippet for supported agents
    pub fn install_for_agent(agent: &str) -> Result<()> {
        match agent {
            "claude" => print_claude_config(),
            "cursor" => print_cursor_config(),
            other => Err(anyhow!(
                "Unsupported agent '{}'. Supported: claude, cursor",
                other
            )),
        }
    }

    fn print_claude_config() -> Result<()> {
        let config = serde_json::json!({
            "mcpServers": {
                "wtcd": {
                    "command": "wtcd",
                    "args": ["mcp"],
                    "env": {}
                }
            }
        });
        println!("# Claude Code MCP Configuration");
        println!("# Add this to ~/.claude.json or run:");
        println!("#   claude mcp add --transport stdio wtcd -- wtcd mcp");
        println!();
        println!("{}", serde_json::to_string_pretty(&config)?);
        Ok(())
    }

    fn print_cursor_config() -> Result<()> {
        let config = serde_json::json!({
            "mcpServers": {
                "wtcd": {
                    "command": "wtcd",
                    "args": ["mcp"]
                }
            }
        });
        println!("# Cursor MCP Configuration");
        println!("# Save to .cursor/mcp.json in your project root");
        println!();
        println!("{}", serde_json::to_string_pretty(&config)?);
        Ok(())
    }
    ```

    Update main.rs to wire the McpInstall command (already planned in Task 1's action).
    The match arm should be:
    ```rust
    Commands::McpInstall { agent } => commands::mcp_install::install_for_agent(&agent),
    ```
  </action>
  <verify>
    <automated>cargo run -p wtcd-cli -- mcp-install claude 2>&1 | head -15</automated>
  </verify>
  <done>`wtcd mcp-install claude` 输出有效的 JSON 配置片段。`wtcd mcp-install cursor` 输出 Cursor 格式。`wtcd mcp-install unknown` 输出错误提示。</done>
</task>

</tasks>

<verification>
```bash
# Verify CLI builds with MCP commands
cargo check -p wtcd-cli

# Verify mcp-install outputs valid JSON
cargo run -p wtcd-cli -- mcp-install claude | python3 -m json.tool

# Verify help text
cargo run -p wtcd-cli -- --help | grep -E "mcp|mcp-install"
```
</verification>

<success_criteria>
- [ ] `wtcd mcp` 启动 MCP Server（stdio 模式，日志输出到 stderr）
- [ ] `wtcd mcp-install claude` 输出可粘贴到 ~/.claude.json 的配置
- [ ] `wtcd mcp-install cursor` 输出 .cursor/mcp.json 格式
- [ ] CLI 使用 tokio::main（async main）
- [ ] cargo check --workspace 通过
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-mcp-cli-agent/999.1-02-SUMMARY.md`
</output>
