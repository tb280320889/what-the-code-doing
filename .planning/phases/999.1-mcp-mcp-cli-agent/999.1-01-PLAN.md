---
phase: "999.1-mcp-mcp-cli-agent"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/wtcd-mcp/Cargo.toml
  - crates/wtcd-mcp/src/lib.rs
  - crates/wtcd-mcp/src/server.rs
  - crates/wtcd-mcp/src/tools/mod.rs
  - crates/wtcd-mcp/src/tools/parse.rs
  - crates/wtcd-mcp/src/tools/run.rs
  - crates/wtcd-mcp/src/tools/check.rs
  - crates/wtcd-mcp/src/tools/mirror.rs
  - crates/wtcd-mcp/src/error.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "wtcd-mcp crate compiles as part of the workspace"
    - "MCP Server starts on stdio transport and responds to initialize"
    - "wtcd_parse tool accepts a file_path and returns parse result JSON"
    - "wtcd_run tool scans a project and returns full analysis JSON"
    - "wtcd_check and wtcd_mirror tools are registered but return 'not implemented'"
  artifacts:
    - path: "crates/wtcd-mcp/Cargo.toml"
      provides: "MCP crate dependency declaration"
    - path: "crates/wtcd-mcp/src/server.rs"
      provides: "WtcdMcpServer struct with ToolRouter + ServerHandler"
    - path: "crates/wtcd-mcp/src/tools/parse.rs"
      provides: "wtcd_parse MCP tool implementation"
    - path: "crates/wtcd-mcp/src/tools/run.rs"
      provides: "wtcd_run MCP tool implementation"
  key_links:
    - from: "crates/wtcd-mcp/src/tools/parse.rs"
      to: "wtcd-adapters"
      via: "register_all_adapters + adapter.parse()"
      pattern: "wtcd_adapters::register_all_adapters"
    - from: "crates/wtcd-mcp/src/tools/run.rs"
      to: "wtcd-scope"
      via: "load_config + scan pipeline"
      pattern: "wtcd_scope::(load_config|scan)"
---

<objective>
创建 wtcd-mcp crate — MCP Server 核心实现，包含 4 个 MCP Tool（2 个完整实现 + 2 个 stub）

**Purpose:** 将 WTCD 的核心能力（解析 + 扫描）通过 MCP 协议暴露给 Agent，实现"工具自动注册到 Agent 上下文"的核心价值

**Output:** 可编译的 wtcd-mcp crate，实现 WtcdMcpServer + 4 个 MCP Tool
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-CONTEXT.md
@.planning/phases/999.1-mcp-mcp-cli-agent/999.1-RESEARCH.md
@crates/wtcd-core/src/types.rs
@crates/wtcd-core/src/adapter.rs
@crates/wtcd-core/src/config.rs
@crates/wtcd-core/src/error.rs
@crates/wtcd-cli/src/commands/run.rs
@Cargo.toml

<interfaces>
From wtcd-core/src/types.rs:
- `FileResult { file_path, confidence, exports, imports, signatures, side_effects, parse_time_ms, error_message }`
- `RunOutput { api_version, files, errors, summary }`
- `RunSummary { total_files, parsed_ok, confidence_low, confidence_none, total_exports, total_imports, elapsed_ms }`
- `ConfidenceBand { High, Low, None }`
- `ExportedSymbol { name, kind, line }`
- `DependencyEdge { source, symbols, kind }`
- `FunctionSignature { name, parameters, return_type }`
- `SideEffect { kind, target, line }`

From wtcd-core/src/adapter.rs:
- `LanguageAdapter` trait: `language_name()`, `file_extensions()`, `parse(&self, source, file_path) -> FileResult`
- `AdapterRegistry`: `new()`, `register()`, `find_adapter(file_path) -> Option<&dyn LanguageAdapter>`

From wtcd-core/src/config.rs:
- `Config { version, repo_root, scope, mirror, output }`
- `ScopeConfig { source_roots, exclude_patterns }`
- `Config::from_yaml(yaml) -> Result<Self>`

From wtcd-core/src/error.rs:
- `WtcdError` enum: ConfigError, ParseError, ScopeError, IoError, YamlError, UnsupportedLanguage
- `Result<T>` type alias

From wtcd-scope/src/lib.rs:
- `pub fn load_config(repo_root: &Path) -> Result<Config>`
- `pub fn scan(repo_root: &Path, scope: &ScopeConfig) -> Result<Vec<PathBuf>>`

From wtcd-adapters:
- `pub fn register_all_adapters() -> Result<AdapterRegistry>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create wtcd-mcp crate scaffold and add to workspace</name>
  <files>Cargo.toml, crates/wtcd-mcp/Cargo.toml, crates/wtcd-mcp/src/lib.rs</files>
  <read_first>Cargo.toml</read_first>
  <action>
    1. Create `crates/wtcd-mcp/Cargo.toml` with:
       - package name: `wtcd-mcp`
       - edition "2021", version "0.1.0"
       - Dependencies:
         - `rmcp = { version = "0.16", features = ["server", "transport-io"] }`
         - `tokio = { version = "1", features = ["full"] }`
         - `schemars = "0.8"` (compatible with rmcp 0.16)
         - `serde = { workspace = true }`
         - `serde_json = { workspace = true }`
         - `anyhow = { workspace = true }`
         - `tracing = { workspace = true }`
         - `tracing-subscriber = { workspace = true }`
         - `wtcd-core = { workspace = true }`
         - `wtcd-scope = { workspace = true }`
         - `wtcd-adapters = { workspace = true }`

    2. Add `"crates/wtcd-mcp"` to the workspace members list in root `Cargo.toml`

    3. Create `crates/wtcd-mcp/src/lib.rs` with module declarations:
       ```rust
       pub mod server;
       pub mod tools;
       pub mod error;
       ```
  </action>
  <verify>
    <automated>cargo check -p wtcd-mcp 2>&1 | tail -5</automated>
  </verify>
  <done>wtcd-mcp crate exists in workspace, cargo check passes (or shows only unimplemented item errors)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement WtcdMcpServer with ServerHandler and 4 MCP Tools</name>
  <files>crates/wtcd-mcp/src/server.rs, crates/wtcd-mcp/src/tools/mod.rs, crates/wtcd-mcp/src/tools/parse.rs, crates/wtcd-mcp/src/tools/run.rs, crates/wtcd-mcp/src/tools/check.rs, crates/wtcd-mcp/src/tools/mirror.rs, crates/wtcd-mcp/src/error.rs</files>
  <read_first>crates/wtcd-mcp/src/lib.rs, crates/wtcd-core/src/adapter.rs, crates/wtcd-core/src/types.rs, crates/wtcd-cli/src/commands/run.rs, crates/wtcd-scope/src/lib.rs</read_first>
  <action>
    Create the following files implementing the MCP Server:

    **crates/wtcd-mcp/src/error.rs:**
    - Define `McpWtcdError` wrapping `WtcdError` for MCP error responses
    - Implement `From<WtcdError>` for `rmcp::ErrorData`

    **crates/wtcd-mcp/src/server.rs:**
    - Define `WtcdMcpServer` struct with `tool_router: ToolRouter<Self>` and `project_root: PathBuf`
    - Implement `new(project_root: PathBuf)` constructor
    - Implement `#[tool_handler] impl ServerHandler for WtcdMcpServer`:
      - `get_info()` returns `ServerInfo` with:
        - `protocol_version: ProtocolVersion::V_2025_03_26`
        - `server_info: Implementation { name: "wtcd-mcp", version: env!("CARGO_PKG_VERSION") }`
        - `instructions: Some("WTCD MCP Server — 提供代码语义解析、路由、检查和镜像生成工具")`
        - `capabilities: ServerCapabilities::builder().enable_tools().build()`

    **crates/wtcd-mcp/src/tools/mod.rs:**
    - Declare modules: parse, run, check, mirror
    - Re-export tool request/response types

    **crates/wtcd-mcp/src/tools/parse.rs (MVP — full implementation):**
    - Define `ParseRequest { file_path: String, language: Option<String> }` with `#[derive(Deserialize, JsonSchema)]`
    - Implement `#[tool(description = "解析单个 TS/JS 文件，提取导出符号、依赖边、函数签名和副作用")]` method on WtcdMcpServer
    - Implementation (per D-02): directly call wtcd-adapters `register_all_adapters()` → `adapter.parse(source, file_path)`
    - Return `CallToolResult::success(vec![Content::text(serde_json::to_string(&file_result)?)])`
    - Wrap synchronous call in `tokio::task::spawn_blocking()`
    - Error handling: if file not found → error; if no adapter → confidence: none with error_message

    **crates/wtcd-mcp/src/tools/run.rs (MVP — full implementation):**
    - Define `RunRequest { project_root: Option<String> }` with `#[derive(Deserialize, JsonSchema)]`
    - Implement `#[tool(description = "扫描整个项目范围内的文件，执行全量语义分析")]` method on WtcdMcpServer
    - Implementation (per D-03): reuse logic from `wtcd-cli/src/commands/run.rs`:
      1. `wtcd_scope::load_config(project_root)`
      2. `wtcd_adapters::register_all_adapters()`
      3. `wtcd_scope::scan(project_root, &config.scope)`
      4. For each file: find adapter → parse → collect FileResult
      5. Build RunSummary → RunOutput
    - Return JSON-serialized RunOutput as CallToolResult
    - Wrap in `tokio::task::spawn_blocking()`

    **crates/wtcd-mcp/src/tools/check.rs (stub):**
    - Implement `#[tool(description = "检测镜像漂移 (Phase 3 能力，当前为 stub)")]` method
    - Return `CallToolResult::error("wtcd_check: Phase 3 capability — not yet implemented")`

    **crates/wtcd-mcp/src/tools/mirror.rs (stub):**
    - Implement `#[tool(description = "生成镜像文件 (Phase 2 能力，当前为 stub)")]` method
    - Return `CallToolResult::error("wtcd_mirror: Phase 2 capability — not yet implemented")`

    **Key patterns from existing code to follow:**
    - wtcd-cli/commands/run.rs shows the exact pipeline: load_config → register_all_adapters → scan → parse each file → build summary
    - ToolRouter pattern: `tool_router: ToolRouter<Self>` field + `#[tool_router]` on impl block + `#[tool_handler]` on ServerHandler impl
    - All tool methods are async, sync wtcd-core calls wrapped in `spawn_blocking`
  </action>
  <verify>
    <automated>cargo build -p wtcd-mcp 2>&1 | tail -10</automated>
  </verify>
  <done>wtcd-mcp crate compiles successfully. WtcdMcpServer implements ServerHandler with 4 tools. wtcd_parse and wtcd_run call real wtcd-core APIs. wtcd_check and wtcd_mirror return stub errors.</done>
</task>

</tasks>

<verification>
```bash
# Verify crate compiles
cargo check -p wtcd-mcp

# Verify all tool definitions are registered
cargo doc -p wtcd-mcp --no-deps 2>&1 | grep -c "wtcd_"

# Verify workspace integrity
cargo check --workspace
```
</verification>

<success_criteria>
- [ ] crates/wtcd-mcp/Cargo.toml exists with rmcp 0.16 + tokio + schemars + wtcd-* deps
- [ ] crates/wtcd-mcp is in workspace members
- [ ] WtcdMcpServer struct compiles with ToolRouter field
- [ ] ServerHandler implemented with correct ServerInfo
- [ ] wtcd_parse tool compiles and calls wtcd-adapters API
- [ ] wtcd_run tool compiles and calls wtcd-scope + wtcd-adapters pipeline
- [ ] wtcd_check tool registered as stub
- [ ] wtcd_mirror tool registered as stub
- [ ] cargo check --workspace passes
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-mcp-cli-agent/999.1-01-SUMMARY.md`
</output>
