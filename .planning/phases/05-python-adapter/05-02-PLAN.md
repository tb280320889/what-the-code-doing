---
phase: 05-python-adapter
plan: 05-02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/fixtures/python/
  - crates/wtcd-adapters/src/py.rs
  - crates/wtcd-tests/src/lib.rs
autonomous: true
requirements:
  - PY-01
  - PY-02
  - PY-03
  - PY-04
  - PY-05
  - PY-06
  - PY-07
  - PY-08
  - PY-09

must_haves:
  truths:
    - "Python 测试 fixture 包含：函数定义、类定义、import 语句、装饰器、__init__.py、__all__"
    - "单元测试覆盖所有 PY-01 ~ PY-09 需求"
    - "集成测试通过 wtcd run 解析 .py 文件并验证 JSON 输出"
    - "语法错误的 Python 文件返回 confidence: low 且不崩溃"
    - "__init__.py 文件被识别为 package marker"
    - "__all__ 声明被正确解析"
    - "@property, @staticmethod, @classmethod 方法类型被检测"
    - "dataclass 和 Pydantic BaseModel 模式被检测"
    - "所有测试通过 (cargo test)"
  artifacts:
    - path: "tests/fixtures/python/hello.py"
      provides: "Basic Python fixture"
      contains: "def greet"
    - path: "tests/fixtures/python/classes.py"
      provides: "Class definition fixture"
      contains: "class Animal"
    - path: "tests/fixtures/python/imports.py"
      provides: "Import patterns fixture"
      contains: "from .relative"
    - path: "tests/fixtures/python/decorators.py"
      provides: "Decorator patterns fixture"
      contains: "@dataclass"
    - path: "tests/fixtures/python/syntax_error.py"
      provides: "Error tolerance fixture"
      contains: "def broken("
  key_links:
    - from: "tests/fixtures/python/"
      to: "crates/wtcd-adapters/src/py.rs"
      via: "parse() calls"
      pattern: "PyAdapter"
---

<objective>
创建 Python 适配器测试套件：fixture 文件、单元测试、集成测试

Purpose: 验证 PyAdapter 正确处理所有 PY-01 ~ PY-09 需求
Output: 全面的测试覆盖 + 所有测试通过
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<implementation>
<step number="1" name="create-test-fixtures">
<action>
1. Create `tests/fixtures/python/` directory
2. Create `hello.py` — basic functions with type annotations:
   ```python
   def greet(name: str) -> str:
       return f"Hello, {name}"
   
   def add(a: int, b: int) -> int:
       return a + b
   ```
3. Create `classes.py` — class definitions with inheritance:
   ```python
   class Animal:
       def __init__(self, name: str):
           self.name = name
   
   class Dog(Animal):
       def bark(self) -> str:
           return "woof"
   ```
4. Create `imports.py` — import patterns:
   ```python
   import os
   import sys as system
   from pathlib import Path
   from typing import List, Dict
   from . import sibling
   from ..parent import utility
   ```
5. Create `decorators.py` — decorator patterns:
   ```python
   from dataclasses import dataclass
   from pydantic import BaseModel
   
   @dataclass
   class Point:
       x: float
       y: float
   
   class User(BaseModel):
       name: str
       email: str
   
   @staticmethod
   def helper() -> None: pass
   
   @property
   def value(self) -> int: return 42
   ```
6. Create `init_package/__init__.py` — package marker test:
   ```python
   __all__ = ["exported_func", "ExportedClass"]
   
   def exported_func(): pass
   class ExportedClass: pass
   ```
7. Create `syntax_error.py` — error tolerance test:
   ```python
   def broken(
       # incomplete function def
   ```
</action>
<commit>feat(05-02): create Python test fixtures</commit>
<verification>
```bash
ls tests/fixtures/python/
```
</verification>
</step>

<step number="2" name="add-unit-tests">
<action>
1. Add unit tests in `crates/wtcd-adapters/src/py.rs` (inside #[cfg(test)] mod tests):
   - test_parse_function_definition: parse hello.py, verify function exports with params and return types
   - test_parse_class_definition: parse classes.py, verify class exports with base classes
   - test_parse_imports: parse imports.py, verify all import patterns including relative imports
   - test_parse_decorators: parse decorators.py, verify decorator metadata on exports
   - test_parse_init_py: parse __init__.py, verify package marker and __all__ detection
   - test_parse_syntax_error: parse syntax_error.py, verify confidence=low, no panic
   - test_parse_property_method: verify @property detection in signatures
   - test_parse_staticmethod_classmethod: verify @staticmethod/@classmethod detection
   - test_parse_dataclass: verify dataclass pattern detection
   - test_parse_pydantic_model: verify Pydantic BaseModel pattern detection
2. Each test uses PyAdapter::new() then adapter.parse(fixture_source, fixture_path)
</action>
<commit>test(05-02): add unit tests for all PY-01 ~ PY-09 requirements</commit>
<verification>
```bash
cargo test -p wtcd-adapters py::
```
</verification>
</step>

<step number="3" name="add-integration-tests">
<action>
1. Add integration test in `crates/wtcd-tests/` or `crates/wtcd-cli/tests/`:
   - test_wtcd_run_python: invoke `wtcd run` on a directory containing .py files
   - Verify JSON output contains Python file results
   - Verify exports, imports, signatures are present
   - Verify confidence is set correctly
2. Verify full test suite passes:
   - `cargo test` across all crates
   - No regressions in existing TS adapter tests
</action>
<commit>test(05-02): add integration tests for wtcd run with Python files</commit>
<verification>
```bash
cargo test
```
</verification>
</step>
</implementation>

<references>
- `tests/fixtures/ts/` — existing TS fixture patterns
- `crates/wtcd-adapters/src/ts.rs` — test patterns to follow
- `.planning/phases/05-python-adapter/05-CONTEXT.md` — Phase decisions
- `crates/wtcd-core/src/types.rs` — type definitions to verify against
</references>
