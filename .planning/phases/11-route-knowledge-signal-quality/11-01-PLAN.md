---
phase: 11-route-knowledge-signal-quality
plan: 01
type: execute
wave: 1
depends_on: [Phase 10]
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

# Phase 11 Plan 01: Route & Knowledge Signal Quality

## Objective

- **What:** 在路由和知识层输出中添加语言覆盖边界和置信度信号
- **Why:** 降低路由误命中率，用户可判断每种语言的能力边界
- **Output:** 路由置信度过滤、语言能力矩阵、低置信度区域展示

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-route-knowledge-signal-quality/11-CONTEXT.md

## Tasks

<task type="code">
  <name>Add language_matrix and low_confidence_modules to KnowledgeResult</name>
  <files>crates/wtcd-core/src/types.rs</files>
  <action>Add `language_matrix: BTreeMap<String, LanguageCapability>` and `low_confidence_modules: Vec<String>` fields to KnowledgeResult. Define LanguageCapability struct with extraction capabilities.</action>
  <verify>cargo check passes</verify>
  <done>KnowledgeResult has language_matrix and low_confidence_modules fields</done>
</task>

<task type="code">
  <name>Add confidence filtering to route query</name>
  <files>crates/wtcd-core/src/index.rs</files>
  <action>In query_route(), filter out ConfidenceBand::None results and deprioritize ConfidenceBand::Low (reduce score by 50%). Add confidence weight to scoring.</action>
  <verify>cargo test index passes</verify>
  <done>Route queries filter None confidence and deprioritize Low</done>
</task>

<task type="code">
  <name>Compute language matrix in knowledge layer</name>
  <files>crates/wtcd-mirror/src/knowledge.rs</files>
  <action>Add compute_language_matrix() function that aggregates per-language extraction capabilities from module results. Add generate_language_matrix_md() to output markdown table. Add low_confidence_modules collection.</action>
  <verify>cargo test knowledge passes</verify>
  <done>Language matrix computed and rendered in knowledge docs</done>
</task>

<task type="code">
  <name>Update build_knowledge_result to populate new fields</name>
  <files>crates/wtcd-mirror/src/knowledge.rs</files>
  <action>In build_knowledge_result(), compute language_matrix and low_confidence_modules from the module results.</action>
  <verify>cargo test passes</verify>
  <done>build_knowledge_result populates language_matrix and low_confidence_modules</done>
</task>

## Verification

1. Run `cargo check` — all types compile
2. Run `cargo test` — all tests pass
3. Verify route queries filter None confidence
4. Verify KnowledgeResult includes language_matrix and low_confidence_modules

## Success Criteria

- [ ] ROUT-01: Route queries use language and confidence signals
- [ ] ROUT-02: Knowledge output includes language capability matrix
- [ ] ROUT-03: Low-confidence and generated/uncertain areas surfaced in knowledge docs
