---
phase: 09-polyglot-adapter-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

# Phase 9 Plan 01: Polyglot Adapter Coverage

## Objective

- **What:** 为 9 种新增语言（C, Zig, Dart, Kotlin, Swift, Java, C#, C++, Rust）创建 LanguageAdapter 实现
- **Why:** 扩展 WTCD 语言支持范围，满足 v0.2.0 里程碑需求
- **Output:** 9 个新的适配器文件，集成到 register_all_adapters()，包含测试 fixtures

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polyglot-adapter-coverage/09-CONTEXT.md

## Tasks

<task type="code">
  <name>Add tree-sitter dependencies</name>
  <files>Cargo.toml, crates/wtcd-adapters/Cargo.toml</files>
  <action>Add tree-sitter-c, tree-sitter-cpp, tree-sitter-c-sharp, tree-sitter-dart, tree-sitter-java, tree-sitter-kotlin, tree-sitter-rust, tree-sitter-swift, tree-sitter-zig to workspace dependencies and adapter crate dependencies</action>
  <verify>cargo check passes with new dependencies</verify>
  <done>All 9 tree-sitter dependencies added and compiling</done>
</task>

<task type="code">
  <name>Implement C adapter</name>
  <files>crates/wtcd-adapters/src/c.rs</files>
  <action>Create CAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, includes, macros, conditional compilation</action>
  <verify>Unit tests pass with C fixtures</verify>
  <done>CAdapter parses C files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Zig adapter</name>
  <files>crates/wtcd-adapters/src/zig.rs</files>
  <action>Create ZigAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, types, imports, comptime blocks</action>
  <verify>Unit tests pass with Zig fixtures</verify>
  <done>ZigAdapter parses Zig files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Dart adapter</name>
  <files>crates/wtcd-adapters/src/dart.rs</files>
  <action>Create DartAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, classes, imports, annotations</action>
  <verify>Unit tests pass with Dart fixtures</verify>
  <done>DartAdapter parses Dart files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Kotlin adapter</name>
  <files>crates/wtcd-adapters/src/kotlin.rs</files>
  <action>Create KotlinAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, classes, imports, annotations, companion objects</action>
  <verify>Unit tests pass with Kotlin fixtures</verify>
  <done>KotlinAdapter parses Kotlin files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Swift adapter</name>
  <files>crates/wtcd-adapters/src/swift.rs</files>
  <action>Create SwiftAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, classes, imports, protocols, extensions</action>
  <verify>Unit tests pass with Swift fixtures</verify>
  <done>SwiftAdapter parses Swift files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Java adapter</name>
  <files>crates/wtcd-adapters/src/java.rs</files>
  <action>Create JavaAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract classes, methods, imports, annotations, generics</action>
  <verify>Unit tests pass with Java fixtures</verify>
  <done>JavaAdapter parses Java files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement C# adapter</name>
  <files>crates/wtcd-adapters/src/csharp.rs</files>
  <action>Create CSharpAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract classes, methods, imports, attributes, generics</action>
  <verify>Unit tests pass with C# fixtures</verify>
  <done>CSharpAdapter parses C# files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement C++ adapter</name>
  <files>crates/wtcd-adapters/src/cpp.rs</files>
  <action>Create CppAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, classes, includes, namespaces, templates</action>
  <verify>Unit tests pass with C++ fixtures</verify>
  <done>CppAdapter parses C++ files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Implement Rust adapter</name>
  <files>crates/wtcd-adapters/src/rust.rs</files>
  <action>Create RustAdapter struct with Mutex<Parser>, implement LanguageAdapter trait, extract functions, structs, enums, use statements, mod declarations, impl blocks, derive macros</action>
  <verify>Unit tests pass with Rust fixtures</verify>
  <done>RustAdapter parses Rust files and extracts exports/imports/signatures/side_effects with confidence</done>
</task>

<task type="code">
  <name>Register all adapters</name>
  <files>crates/wtcd-adapters/src/lib.rs</files>
  <action>Add mod declarations for all 9 new adapters and register them in register_all_adapters()</action>
  <verify>cargo test passes, all adapters registered</verify>
  <done>All 9 adapters registered and discoverable by extension</done>
</task>

<task type="code">
  <name>Create test fixtures</name>
  <files>tests/fixtures/c/, tests/fixtures/zig/, tests/fixtures/dart/, tests/fixtures/kotlin/, tests/fixtures/swift/, tests/fixtures/java/, tests/fixtures/csharp/, tests/fixtures/cpp/, tests/fixtures/rust/</files>
  <action>Create 5 fixture files per language: hello, imports, classes/types, syntax_error, complex</action>
  <verify>Fixture files exist and contain representative code</verify>
  <done>45 fixture files created (5 per language × 9 languages)</done>
</task>

<task type="code">
  <name>Add integration tests</name>
  <files>crates/wtcd-tests/</files>
  <action>Add integration tests that run wtcd run on multi-language repositories and verify output</action>
  <verify>Integration tests pass</verify>
  <done>Integration tests verify wtcd run works with all 13 languages</done>
</task>

## Verification

1. Run `cargo test` — all unit tests pass
2. Run `cargo build` — compilation succeeds
3. Run `wtcd run` on test fixtures — all 9 languages parsed with confidence
4. Verify parse_time_ms < 100ms per file for all languages
5. Verify no regressions in existing TS/JS/Python/Go adapters

## Success Criteria

- [ ] ADPT-01: Rust files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-02: Dart files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-03: Java files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-04: Kotlin files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-05: Swift files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-06: C++ files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-07: C# files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-08: C files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-09: Zig files parsed via `wtcd run` with exports/imports/signatures/side effects and confidence
- [ ] ADPT-10: Scanner coverage for all new language extensions
- [ ] ADPT-11: Adapter registry auto-selection works without new CLI commands
