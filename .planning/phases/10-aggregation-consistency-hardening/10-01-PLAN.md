---
phase: 10-aggregation-consistency-hardening
plan: 01
type: execute
wave: 1
depends_on: [Phase 9]
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths: []
  artifacts: []
  key_links: []
---

# Phase 10 Plan 01: Aggregation Consistency Hardening

## Objective

- **What:** 确保跨 13 种语言的依赖聚合、模块图构建、漂移检测和置信度分级行为一致
- **Why:** 新增 9 种语言后，import 归一和模块图需要修复以支持所有语言
- **Output:** 扩展 resolve_import_path、修复 build_module_graph、添加 confidence rollup、标记 generated symbols

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-aggregation-consistency-hardening/10-CONTEXT.md

## Tasks

<task type="code">
  <name>Add is_generated and confidence to ExportedSymbol</name>
  <files>crates/wtcd-core/src/types.rs</files>
  <action>Add `is_generated: bool` (default false) and `confidence: ConfidenceBand` (default High) fields to ExportedSymbol struct. Update all constructors/initializations.</action>
  <verify>cargo check passes</verify>
  <done>ExportedSymbol has is_generated and confidence fields</done>
</task>

<task type="code">
  <name>Add confidence to ModuleResult</name>
  <files>crates/wtcd-core/src/types.rs</files>
  <action>Add `confidence: ConfidenceBand` field to ModuleResult struct. Default to High.</action>
  <verify>cargo check passes</verify>
  <done>ModuleResult has confidence field</done>
</task>

<task type="code">
  <name>Extend resolve_import_path for all languages</name>
  <files>crates/wtcd-core/src/depgraph.rs</files>
  <action>Add a language extension map (const HashMap or match) that resolves imports for Rust (.rs), Java (.java), Kotlin (.kt,.kts), Swift (.swift), Dart (.dart), C (.c,.h), C++ (.cpp,.cc,.cxx,.hpp,.h,.hh,.hxx), C# (.cs), Zig (.zig). Update resolve_import_path() to use this map based on the importer file's extension.</action>
  <verify>cargo test depgraph passes</verify>
  <done>resolve_import_path handles all 13 language extensions</done>
</task>

<task type="code">
  <name>Fix build_module_graph to use path prefix matching</name>
  <files>crates/wtcd-mirror/src/module.rs</files>
  <action>Replace substring matching in build_module_graph() with canonical path prefix comparison. Normalize module IDs to consistent path format before comparison.</action>
  <verify>cargo test module passes</verify>
  <done>Module graph uses path prefix matching instead of substring</done>
</task>

<task type="code">
  <name>Add confidence rollup to aggregate_module</name>
  <files>crates/wtcd-mirror/src/module.rs</files>
  <action>In aggregate_module(), compute module-level confidence as the minimum confidence across all files in the module. Set it on ModuleResult.confidence.</action>
  <verify>cargo test module passes</verify>
  <done>Module confidence = min(file confidences)</done>
</task>

<task type="code">
  <name>Update adapters to set ExportedSymbol confidence and is_generated</name>
  <files>crates/wtcd-adapters/src/*.rs</files>
  <action>For all 12 adapters, when creating ExportedSymbol instances, set confidence based on parse confidence and is_generated=true for generated/uncertain symbols (e.g., FFI bindings, macro expansions, auto-generated code).</action>
  <verify>cargo test passes, all 207+ tests green</verify>
  <done>All adapters set ExportedSymbol.confidence and .is_generated</done>
</task>

<task type="code">
  <name>Add integration tests for cross-language consistency</name>
  <files>crates/wtcd-tests/tests/</files>
  <action>Add tests verifying: (1) import resolution works for new languages, (2) module graph produces consistent results, (3) confidence rollup is correct, (4) generated symbols are marked.</action>
  <verify>New tests pass</verify>
  <done>Integration tests for aggregation consistency pass</done>
</task>

## Verification

1. Run `cargo check` — all types compile
2. Run `cargo test` — all tests pass (including new consistency tests)
3. Verify `resolve_import_path()` handles all 13 language extensions
4. Verify `build_module_graph()` uses path prefix matching
5. Verify `ModuleResult.confidence` is set correctly (min rollup)
6. Verify `ExportedSymbol.is_generated` and `.confidence` are populated

## Success Criteria

- [ ] CONS-01: Import normalization across all supported languages
- [ ] CONS-02: Module aggregation semantically consistent across all 13 languages
- [ ] CONS-03: Stable drift classification with preprocessor handling
- [ ] CONS-04: Generated/uncertain symbols explicitly marked with confidence
