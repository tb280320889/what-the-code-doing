---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/wtcd-scope/src/lib.rs
  - crates/wtcd-scope/src/scanner.rs
  - crates/wtcd-scope/src/scope.rs
autonomous: true
requirements:
  - SCOP-01
  - SCOP-02
  - SCOP-03

must_haves:
  truths:
    - "ScopeConfig 可以从 YAML 字符串解析"
    - "scanner 枚举所有 source_roots 下的文件"
    - "exclude_patterns 正确过滤文件"
    - "scanner 支持 .gitignore 规则（通过 ignore crate）"
    - "返回的文件列表是确定性的（相同配置+目录 = 相同结果）"
    - "文件扩展名过滤只返回 .ts/.tsx/.js/.jsx"
  artifacts:
    - path: "crates/wtcd-scope/src/lib.rs"
      provides: "Public API exports"
      contains: "pub mod scanner"
    - path: "crates/wtcd-scope/src/scanner.rs"
      provides: "File enumeration with glob filtering"
      contains: "pub fn scan"
    - path: "crates/wtcd-scope/src/scope.rs"
      provides: "Config parsing and scope resolution"
      contains: "pub fn load_config"
  key_links:
    - from: "crates/wtcd-scope/src/scope.rs"
      to: "wtcd_core::config::Config"
      via: "uses Config struct"
      pattern: "wtcd_core::config"
    - from: "crates/wtcd-scope/src/scanner.rs"
      to: "ignore::WalkBuilder"
      via: "uses ignore crate for file walking"
      pattern: "ignore::"
    - from: "scanner.rs"
      to: "scope.rs"
      via: "scanner takes ScopeConfig as input"
      pattern: "ScopeConfig"
---

<objective>
实现配置解析和文件范围管理

Purpose: 让用户可以通过 anrsm.yaml 配置文件范围规则，系统确定性地枚举纳入范围的文件
Output: wtcd-scope crate — config loading + file scanning with glob/gitignore filtering
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Depends on Plan 01 types — same wave but independent file set -->

From wtcd-core (created by Plan 01):
```rust
// crates/wtcd-core/src/config.rs
pub struct Config { pub version: u32, pub repo_root: String, pub scope: ScopeConfig, ... }
pub struct ScopeConfig { pub source_roots: Vec<String>, pub exclude_patterns: Vec<String> }
impl Config { pub fn from_yaml(yaml: &str) -> Result<Self> }

// crates/wtcd-core/src/error.rs
pub enum WtcdError { ConfigError(String), ScopeError(String), ... }
pub type Result<T> = std::result::Result<T, WtcdError>;
```

From 01-CONTEXT.md:
- D-09: 标准 glob 语法，与 ignore crate 兼容
- D-10: 无 anrsm.yaml 时拒绝运行
- D-11: Phase 1 不定义模块归属

From 01-RESEARCH.md:
- ignore crate 0.4 WalkBuilder API: WalkBuilder::new(path).git_ignore(true).build()
- globset crate for custom include/exclude patterns
- 需要 .ts, .tsx, .js, .jsx 扩展名过滤
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement config loading (scope.rs)</name>
  <files>crates/wtcd-scope/src/scope.rs, crates/wtcd-scope/src/lib.rs</files>
  <behavior>
    - Test 1: load_config 从有效的 YAML 字符串成功解析 Config
    - Test 2: load_config 在无效 YAML 时返回 YamlError
    - Test 3: Config 在缺少 required 字段时返回解析错误
    - Test 4: source_roots 和 exclude_patterns 被正确保留
  </behavior>
  <read_first>
    crates/wtcd-core/src/config.rs
    crates/wtcd-core/src/error.rs
    crates/wtcd-scope/src/lib.rs
    examples/anrsm.example.yaml
  </read_first>
  <action>
    1. 更新 `crates/wtcd-scope/src/lib.rs`：
    ```rust
    pub mod scanner;
    pub mod scope;

    pub use scope::load_config;
    pub use scanner::scan;
    ```

    2. 创建 `crates/wtcd-scope/src/scope.rs`：
    ```rust
    use std::path::{Path, PathBuf};
    use wtcd_core::config::Config;
    use wtcd_core::error::{Result, WtcdError};

    /// Config file name (D-08, D-09)
    pub const CONFIG_FILENAME: &str = "anrsm.yaml";

    /// Load configuration from a directory
    /// Returns error if anrsm.yaml doesn't exist (D-10)
    pub fn load_config(repo_root: &Path) -> Result<Config> {
        let config_path = repo_root.join(CONFIG_FILENAME);
        if !config_path.exists() {
            return Err(WtcdError::ConfigError(
                format!("No {} found. Run `wtcd init` first.", CONFIG_FILENAME)
            ));
        }
        let yaml = std::fs::read_to_string(&config_path)
            .map_err(|e| WtcdError::ConfigError(format!("Cannot read {}: {}", CONFIG_FILENAME, e)))?;
        Config::from_yaml(&yaml)
    }

    /// Find the config file by searching up from given directory
    pub fn find_config(start_dir: &Path) -> Option<PathBuf> {
        let mut dir = start_dir.to_path_buf();
        loop {
            let candidate = dir.join(CONFIG_FILENAME);
            if candidate.exists() {
                return Some(candidate);
            }
            if !dir.pop() {
                return None;
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && cargo build -p wtcd-scope</automated>
  </verify>
  <done>
    load_config 从 YAML 文件解析 Config，缺失文件时返回 ConfigError
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement file scanner (scanner.rs)</name>
  <files>crates/wtcd-scope/src/scanner.rs</files>
  <behavior>
    - Test 1: scan 枚举指定 source_roots 下的所有 .ts/.js 文件
    - Test 2: exclude_patterns 正确过滤匹配的文件
    - Test 3: .gitignore 中的规则被尊重
    - Test 4: 非 .ts/.tsx/.js/.jsx 文件被过滤掉
    - Test 5: 结果按路径排序（确定性）
  </behavior>
  <read_first>
    crates/wtcd-scope/src/scope.rs
    crates/wtcd-core/src/config.rs (ScopeConfig)
    crates/wtcd-core/src/error.rs
  </read_first>
  <action>
    创建 `crates/wtcd-scope/src/scanner.rs`：

    ```rust
    use std::path::{Path, PathBuf};
    use ignore::WalkBuilder;
    use globset::{GlobSet, GlobSetBuilder, Glob};
    use wtcd_core::config::ScopeConfig;
    use wtcd_core::error::{Result, WtcdError};

    /// Supported file extensions (Phase 1)
    const SUPPORTED_EXTENSIONS: &[&str] = &["ts", "tsx", "js", "jsx"];

    /// Scan source roots for supported files, applying exclude patterns and .gitignore
    pub fn scan(repo_root: &Path, scope: &ScopeConfig) -> Result<Vec<PathBuf>> {
        let exclude_set = build_exclude_globset(&scope.exclude_patterns)?;
        let mut results = Vec::new();

        for source_root in &scope.source_roots {
            let root_path = repo_root.join(source_root);
            if !root_path.exists() {
                continue;
            }

            let walker = WalkBuilder::new(&root_path)
                .git_ignore(true)
                .hidden(true)
                .build();

            for entry in walker.flatten() {
                let path = entry.path();

                // Skip directories
                if path.is_dir() {
                    continue;
                }

                // Check file extension
                let ext = path.extension().and_then(|e| e.to_str()).unwrap_or("");
                if !SUPPORTED_EXTENSIONS.contains(&ext) {
                    continue;
                }

                // Check exclude patterns (relative to repo root)
                let relative = path.strip_prefix(repo_root)
                    .unwrap_or(path);
                if exclude_set.is_match(relative) {
                    continue;
                }

                results.push(path.to_path_buf());
            }
        }

        // Sort for determinism (SCOP-02)
        results.sort();
        Ok(results)
    }

    fn build_exclude_globset(patterns: &[String]) -> Result<GlobSet> {
        let mut builder = GlobSetBuilder::new();
        for pattern in patterns {
            let glob = Glob::new(pattern)
                .map_err(|e| WtcdError::ScopeError(format!("Invalid glob '{}': {}", pattern, e)))?;
            builder.add(glob);
        }
        builder.build()
            .map_err(|e| WtcdError::ScopeError(format!("Failed to build glob set: {}", e)))
    }
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && cargo build -p wtcd-scope && cargo test -p wtcd-scope 2>&1 || echo "Build OK, no tests yet"</automated>
  </verify>
  <done>
    scan() 返回确定性的文件列表，遵循 exclude_patterns 和 .gitignore，只包含 .ts/.tsx/.js/.jsx
  </done>
</task>

</tasks>

<verification>
- cargo build -p wtcd-scope 编译通过
- load_config 在缺失 anrsm.yaml 时返回错误
- scan 在 fixture 目录返回正确的文件列表
- 排序确定性验证
</verification>

<success_criteria>
- [ ] scope.rs 实现 load_config，从 YAML 解析 Config
- [ ] scope.rs 实现 find_config，向上搜索 anrsm.yaml
- [ ] load_config 在无配置文件时返回 ConfigError
- [ ] scanner.rs 实现 scan，枚举 source_roots 下的文件
- [ ] exclude_patterns 通过 globset 正确过滤
- [ ] .gitignore 规则被 ignore crate 尊重
- [ ] 只返回 .ts/.tsx/.js/.jsx 扩展名的文件
- [ ] 结果按路径排序（确定性）
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
