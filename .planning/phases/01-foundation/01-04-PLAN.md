---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - crates/wtcd-cli/src/main.rs
  - crates/wtcd-cli/src/commands/init.rs
  - crates/wtcd-cli/src/commands/run.rs
  - crates/wtcd-cli/src/output.rs
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-06

must_haves:
  truths:
    - "wtcd init 在目标仓库创建 anrsm.yaml 配置文件"
    - "anrsm.yaml 包含 scope.source_roots 和 scope.exclude_patterns"
    - "wtcd run 解析所有 scoped 文件并输出 JSON"
    - "JSON 输出包含 api_version, files[], errors[], summary"
    - "summary 包含 total_files, parsed_ok, confidence_low, confidence_none, total_exports, total_imports, elapsed_ms"
    - "wtcd init 在已有 anrsm.yaml 时显示错误"
    - "所有 CLI 输出为 JSON 格式（--json flag 或默认）"
  artifacts:
    - path: "crates/wtcd-cli/src/main.rs"
      provides: "CLI entry point with clap"
      contains: "Commands::Init"
    - path: "crates/wtcd-cli/src/commands/init.rs"
      provides: "Init command implementation"
      contains: "pub fn run_init"
    - path: "crates/wtcd-cli/src/commands/run.rs"
      provides: "Run command implementation"
      contains: "pub fn run_analysis"
    - path: "crates/wtcd-cli/src/output.rs"
      provides: "JSON output formatting"
      contains: "pub fn format_json"
  key_links:
    - from: "crates/wtcd-cli/src/commands/run.rs"
      to: "wtcd_scope::scan"
      via: "calls scan() for file enumeration"
      pattern: "scan("
    - from: "crates/wtcd-cli/src/commands/run.rs"
      to: "wtcd_adapters::register_all_adapters"
      via: "registers language adapters"
      pattern: "register_all_adapters"
    - from: "crates/wtcd-cli/src/commands/run.rs"
      to: "LanguageAdapter::parse"
      via: "calls parse() per file"
      pattern: "\\.parse("
---

<objective>
实现 CLI init 和 run 命令，完成端到端的分析流水线

Purpose: 让用户能通过 `wtcd init` 初始化配置，`wtcd run` 分析仓库并获取 JSON 输出
Output: 完整的 CLI — init 创建配置，run 执行 Config→Scope→Scan→Parse→JSON 流水线
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Depends on Plans 01, 02, 03 types and APIs -->

From wtcd-core (Plan 01):
```rust
pub struct Config { pub version: u32, pub scope: ScopeConfig, ... }
pub struct ScopeConfig { pub source_roots: Vec<String>, pub exclude_patterns: Vec<String> }
pub struct RunOutput { pub api_version: String, pub files: Vec<FileResult>, pub errors: Vec<String>, pub summary: RunSummary }
pub struct FileResult { pub file_path, pub confidence, pub exports, pub imports, pub signatures, pub side_effects, pub parse_time_ms, pub error_message }
pub struct RunSummary { pub total_files, pub parsed_ok, pub confidence_low, pub confidence_none, pub total_exports, pub total_imports, pub elapsed_ms }
```

From wtcd-scope (Plan 02):
```rust
pub fn scan(repo_root: &Path, scope: &ScopeConfig) -> Result<Vec<PathBuf>>
pub fn load_config(repo_root: &Path) -> Result<Config>
```

From wtcd-adapters (Plan 03):
```rust
pub fn register_all_adapters() -> Result<AdapterRegistry>
// AdapterRegistry: fn find_adapter(&self, file_path: &str) -> Option<&dyn LanguageAdapter>
```

From 01-CONTEXT.md:
- D-01: 顶层 files[], errors, summary
- D-04: api_version: "1"
- D-15: 单文件解析失败不影响退出码
- D-26: wtcd-cli 包含 main.rs, commands/init.rs, commands/run.rs, output.rs
- D-29: 配置模板用 include_str! 嵌入
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement init command</name>
  <files>crates/wtcd-cli/src/commands/init.rs, crates/wtcd-cli/src/commands/mod.rs, crates/wtcd-cli/src/main.rs</files>
  <behavior>
    - Test 1: wtcd init 创建 anrsm.yaml（含 scope/mirror/output 块）
    - Test 2: wtcd init 在已有 anrsm.yaml 时返回错误
    - Test 3: 生成的 YAML 包含合理的默认 source_roots 和 exclude_patterns
  </behavior>
  <read_first>
    crates/wtcd-cli/src/main.rs
    crates/wtcd-core/src/config.rs
    examples/anrsm.example.yaml
  </read_first>
  <action>
    1. 创建 `crates/wtcd-cli/src/commands/mod.rs`：
    ```rust
    pub mod init;
    pub mod run;
    ```

    2. 创建 `crates/wtcd-cli/src/commands/init.rs`：
    ```rust
    use std::path::Path;
    use wtcd_core::config::{Config, ScopeConfig, MirrorConfig, OutputConfig};
    use wtcd_core::error::{Result, WtcdError};

    /// Default config template (D-29: include_str! style, but inline for simplicity)
    const DEFAULT_CONFIG: &str = r#"# WTCD — What The Code Doing
version: 1
repo_root: .

scope:
  source_roots:
    - src
    - app
    - lib
    - packages
  exclude_patterns:
    - "**/node_modules/**"
    - "**/dist/**"
    - "**/build/**"
    - "**/.next/**"
    - "**/coverage/**"
    - "**/*.d.ts"
    - "**/*.min.js"

mirror: {}

output:
  format: json
"#;

    pub fn run_init(repo_root: &Path) -> Result<()> {
        let config_path = repo_root.join("anrsm.yaml");

        if config_path.exists() {
            return Err(WtcdError::ConfigError(
                "anrsm.yaml already exists. Delete it first to reinitialize.".to_string()
            ));
        }

        std::fs::write(&config_path, DEFAULT_CONFIG)
            .map_err(|e| WtcdError::ConfigError(format!("Cannot write anrsm.yaml: {}", e)))?;

        // Create .anrsm directory
        let anrsm_dir = repo_root.join(".anrsm");
        if !anrsm_dir.exists() {
            std::fs::create_dir_all(&anrsm_dir)?;
        }

        // JSON output (D-01, CORE-06)
        let output = serde_json::json!({
            "status": "ok",
            "message": "Initialized wtcd configuration",
            "config_path": config_path.display().to_string(),
        });
        println!("{}", serde_json::to_string_pretty(&output).unwrap());

        Ok(())
    }
    ```

    3. 更新 `crates/wtcd-cli/src/main.rs`（添加 init/run 子命令调用）：
    ```rust
    mod commands;

    use clap::Parser;
    use std::path::PathBuf;

    #[derive(Parser)]
    #[command(name = "wtcd", version = env!("CARGO_PKG_VERSION"), about = "What The Code Doing — AI-native repo semantic mirror")]
    struct Cli {
        /// Output directory root
        #[arg(long, default_value = ".")]
        root: PathBuf,

        #[command(subcommand)]
        command: Commands,
    }

    #[derive(clap::Subcommand)]
    enum Commands {
        /// Initialize wtcd configuration in current repo
        Init {},
        /// Run analysis on scoped files
        Run {},
    }

    fn main() {
        let cli = Cli::parse();
        let result = match cli.command {
            Commands::Init {} => commands::init::run_init(&cli.root),
            Commands::Run {} => commands::run::run_analysis(&cli.root),
        };

        if let Err(e) = result {
            let error_json = serde_json::json!({
                "status": "error",
                "message": e.to_string(),
            });
            eprintln!("{}", serde_json::to_string_pretty(&error_json).unwrap());
            std::process::exit(1);
        }
    }
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && cargo build -p wtcd-cli</automated>
  </verify>
  <done>
    wtcd init 创建 anrsm.yaml 和 .anrsm 目录，已有文件时报错
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run command with JSON output</name>
  <files>crates/wtcd-cli/src/commands/run.rs, crates/wtcd-cli/src/output.rs</files>
  <behavior>
    - Test 1: wtcd run 在含 anrsm.yaml 的目录输出合法 JSON
    - Test 2: JSON 包含 api_version="1", files[], errors[], summary
    - Test 3: summary 的 total_files 等于 files 数组长度
    - Test 4: 解析失败的文件在 files 数组中以 confidence=none 出现
    - Test 5: wtcd run 无 anrsm.yaml 时输出错误 JSON
  </behavior>
  <read_first>
    crates/wtcd-cli/src/commands/init.rs
    crates/wtcd-scope/src/lib.rs
    crates/wtcd-adapters/src/lib.rs
    crates/wtcd-core/src/types.rs
  </read_first>
  <action>
    1. 创建 `crates/wtcd-cli/src/commands/run.rs`：
    ```rust
    use std::path::Path;
    use std::time::Instant;
    use wtcd_core::types::*;
    use wtcd_adapters::register_all_adapters;
    use crate::output::format_json;

    pub fn run_analysis(repo_root: &Path) -> wtcd_core::error::Result<()> {
        let overall_start = Instant::now();

        // 1. Load config (D-10: must have anrsm.yaml)
        let config = wtcd_scope::load_config(repo_root)?;

        // 2. Register adapters
        let registry = register_all_adapters()
            .map_err(|e| wtcd_core::error::WtcdError::ConfigError(e.to_string()))?;

        // 3. Scan files
        let files = wtcd_scope::scan(repo_root, &config.scope)?;

        // 4. Parse each file
        let mut file_results = Vec::new();
        for file_path in &files {
            let relative = file_path.strip_prefix(repo_root)
                .unwrap_or(file_path)
                .to_string_lossy()
                .to_string();

            let adapter = registry.find_adapter(&relative);
            let result = match adapter {
                Some(adapter) => {
                    match std::fs::read_to_string(file_path) {
                        Ok(source) => adapter.parse(&source, &relative),
                        Err(e) => FileResult {
                            file_path: relative.clone(),
                            confidence: ConfidenceBand::None,
                            exports: vec![],
                            imports: vec![],
                            signatures: vec![],
                            side_effects: vec![],
                            parse_time_ms: 0,
                            error_message: Some(format!("Cannot read file: {}", e)),
                        },
                    }
                }
                None => {
                    FileResult {
                        file_path: relative.clone(),
                        confidence: ConfidenceBand::None,
                        exports: vec![],
                        imports: vec![],
                        signatures: vec![],
                        side_effects: vec![],
                        parse_time_ms: 0,
                        error_message: Some("No language adapter found".to_string()),
                    }
                }
            };
            file_results.push(result);
        }

        // 5. Build summary (D-07)
        let total_exports: usize = file_results.iter().map(|r| r.exports.len()).sum();
        let total_imports: usize = file_results.iter().map(|r| r.imports.len()).sum();
        let summary = RunSummary {
            total_files: file_results.len(),
            parsed_ok: file_results.iter().filter(|r| r.confidence == ConfidenceBand::High).count(),
            confidence_low: file_results.iter().filter(|r| r.confidence == ConfidenceBand::Low).count(),
            confidence_none: file_results.iter().filter(|r| r.confidence == ConfidenceBand::None).count(),
            total_exports,
            total_imports,
            elapsed_ms: overall_start.elapsed().as_millis() as u64,
        };

        // 6. Output JSON (D-01, D-04, CORE-06)
        let output = RunOutput {
            api_version: "1".to_string(),
            files: file_results,
            errors: vec![],
            summary,
        };

        format_json(&output);
        Ok(())
    }
    ```

    2. 创建 `crates/wtcd-cli/src/output.rs`：
    ```rust
    use wtcd_core::types::RunOutput;

    /// Format and print run output as JSON (CORE-06)
    pub fn format_json(output: &RunOutput) {
        match serde_json::to_string_pretty(output) {
            Ok(json) => println!("{}", json),
            Err(e) => {
                eprintln!("{{\"error\": \"Failed to serialize output: {}\"}}", e);
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && cargo build -p wtcd-cli && echo "Build OK"</automated>
  </verify>
  <done>
    wtcd run 执行完整流水线：load_config → scan → parse → JSON output，退出码始终为 0
  </done>
</task>

</tasks>

<verification>
- cargo build -p wtcd-cli 编译通过
- wtcd init 创建 anrsm.yaml + .anrsm 目录
- wtcd run 在有 anrsm.yaml 的目录输出合法 JSON
- JSON 结构：api_version, files[], errors[], summary
- 解析失败文件出现在 files 数组中（非 errors 数组）
- 退出码为 0（即使有解析错误）
</verification>

<success_criteria>
- [ ] init.rs 实现 run_init，创建 anrsm.yaml（含 scope/mirror/output）
- [ ] init.rs 在已有 anrsm.yaml 时返回 ConfigError
- [ ] run.rs 实现 run_analysis，执行 Config→Scan→Parse→JSON 流水线
- [ ] run.rs 注册所有适配器并按扩展名选择
- [ ] run.rs 对无法读取的文件返回 confidence=none
- [ ] output.rs 输出格式化的 JSON（api_version="1"）
- [ ] main.rs clap 子命令 init/run 正确路由
- [ ] 所有错误输出为 JSON 格式
- [ ] 退出码：成功=0，错误=1
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
