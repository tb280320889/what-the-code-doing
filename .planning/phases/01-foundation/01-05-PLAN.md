---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 04
files_modified:
  - tests/fixtures/ts/basic_exports.ts
  - tests/fixtures/ts/type_exports.ts
  - tests/fixtures/ts/named_imports.ts
  - tests/fixtures/ts/commonjs.ts
  - tests/fixtures/ts/side_effects.ts
  - tests/fixtures/ts/broken_syntax.ts
  - tests/fixtures/ts/function_signatures.ts
  - tests/fixtures/js/basic_exports.js
  - tests/integration_init.rs
  - tests/integration_run.rs
autonomous: true
requirements:
  - CORE-01
  - CORE-06
  - LANG-01
  - LANG-02
  - LANG-03
  - LANG-04
  - LANG-05
  - LANG-06

must_haves:
  truths:
    - "cargo test 通过所有集成测试"
    - "golden fixture basic_exports.ts 提取正确的导出符号"
    - "golden fixture broken_syntax.ts 返回 confidence=low 且有部分提取"
    - "golden fixture side_effects.ts 检测到 fs/fetch/console 调用"
    - "JSON 输出可通过 serde_json 解析"
    - "功能签名 fixture 正确提取参数类型和返回类型"
    - "JS fixture 也正确解析（非 TS 独占）"
  artifacts:
    - path: "tests/fixtures/ts/basic_exports.ts"
      provides: "Golden test: exports"
    - path: "tests/fixtures/ts/broken_syntax.ts"
      provides: "Golden test: error recovery"
    - path: "tests/fixtures/ts/side_effects.ts"
      provides: "Golden test: side effects"
    - path: "tests/fixtures/ts/function_signatures.ts"
      provides: "Golden test: signatures"
    - path: "tests/fixtures/js/basic_exports.js"
      provides: "Golden test: JS parsing"
    - path: "tests/integration_run.rs"
      provides: "Integration test for full pipeline"
  key_links:
    - from: "tests/integration_run.rs"
      to: "wtcd_adapters::register_all_adapters"
      via: "uses adapter registry"
      pattern: "register_all_adapters"
    - from: "tests/integration_run.rs"
      to: "tests/fixtures/"
      via: "reads fixture files"
      pattern: "fixtures"
---

<objective>
创建 golden test fixtures 和集成测试，验证 Phase 1 全部功能

Purpose: 确保 tree-sitter 解析、提取逻辑、JSON 输出和 CLI 全部正确工作，为后续 phase 提供回归保护
Output: 完整的 test suite + golden fixtures，`cargo test` 全部通过
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Depends on all prior plans -->

From Plans 01-04:
```rust
// All public APIs from 4 crates
wtcd_core::types::* // RunOutput, FileResult, ConfidenceBand, etc.
wtcd_core::adapter::* // LanguageAdapter, AdapterRegistry
wtcd_scope::scan, wtcd_scope::load_config
wtcd_adapters::register_all_adapters
```

From 01-RESEARCH.md (Test Strategy):
- basic_exports.ts → 3 exports, confidence=high
- type_exports.ts → type/interface/enum exports
- named_imports.ts → 3 imports
- commonjs.ts → 2 require() imports
- side_effects.ts → 4 side_effects (fs, fetch, console)
- broken_syntax.ts → confidence=low, partial extraction
- function_signatures.ts → 2 signatures with full types
- mixed_module.ts → both ESM + CJS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden test fixtures</name>
  <files>tests/fixtures/ts/basic_exports.ts, tests/fixtures/ts/type_exports.ts, tests/fixtures/ts/named_imports.ts, tests/fixtures/ts/commonjs.ts, tests/fixtures/ts/side_effects.ts, tests/fixtures/ts/broken_syntax.ts, tests/fixtures/ts/function_signatures.ts, tests/fixtures/js/basic_exports.js</files>
  <read_first>
    crates/wtcd-adapters/src/ts.rs
  </read_first>
  <action>
    1. 创建 `tests/fixtures/ts/basic_exports.ts`：
    ```typescript
    export function greet(name: string): string {
      return `Hello, ${name}!`;
    }

    export class UserService {
      getUser(id: number) { return null; }
    }

    export const MAX_RETRIES = 3;
    ```

    2. 创建 `tests/fixtures/ts/type_exports.ts`：
    ```typescript
    export type UserId = string;

    export interface AppConfig {
      port: number;
      host: string;
    }

    export enum Status {
      Active = "active",
      Inactive = "inactive",
    }
    ```

    3. 创建 `tests/fixtures/ts/named_imports.ts`：
    ```typescript
    import { UserService, AppConfig } from "./services";
    import type { UserId } from "./types";
    import * as utils from "../utils";
    ```

    4. 创建 `tests/fixtures/ts/commonjs.ts`：
    ```typescript
    const fs = require("fs");
    const path = require("path");
    ```

    5. 创建 `tests/fixtures/ts/side_effects.ts`：
    ```typescript
    export function readFile(path: string) {
      return fs.readFileSync(path, "utf-8");
    }

    export async function fetchData(url: string) {
      return fetch(url);
    }

    export function log(msg: string) {
      console.log(msg);
    }

    export function storeLocal(key: string, value: string) {
      localStorage.setItem(key, value);
    }
    ```

    6. 创建 `tests/fixtures/ts/broken_syntax.ts`：
    ```typescript
    export function incomplete(
    // Missing closing paren and body

    export class
    // Missing class name

    const x = ;
    ```

    7. 创建 `tests/fixtures/ts/function_signatures.ts`：
    ```typescript
    export function add(a: number, b: number): number {
      return a + b;
    }

    export function greet(name: string, greeting?: string): string {
      return `${greeting ?? "Hello"}, ${name}!`;
    }
    ```

    8. 创建 `tests/fixtures/js/basic_exports.js`：
    ```javascript
    export function double(x) {
      return x * 2;
    }

    export class Calculator {
      add(a, b) { return a + b; }
    }

    export const PI = 3.14159;
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && ls tests/fixtures/ts/ tests/fixtures/js/ 2>&1 | wc -l</automated>
  </verify>
  <done>
    All 8 golden fixture files created in tests/fixtures/
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write integration tests</name>
  <files>tests/integration_init.rs, tests/integration_run.rs</files>
  <behavior>
    - Test 1: integration_run — basic_exports.ts 解析结果包含 3 个导出 + confidence=high
    - Test 2: integration_run — broken_syntax.ts 返回 confidence=low 或 none
    - Test 3: integration_run — side_effects.ts 检测到至少 2 个 side_effects
    - Test 4: integration_run — JSON 输出可解析为 RunOutput
    - Test 5: integration_run — JS 文件同样正确解析
    - Test 6: integration_init — 在空目录 init 创建 anrsm.yaml
  </behavior>
  <read_first>
    crates/wtcd-adapters/src/ts.rs
    crates/wtcd-adapters/src/lib.rs
    crates/wtcd-core/src/types.rs
  </read_first>
  <action>
    1. 创建 `tests/integration_run.rs`：
    ```rust
    use wtcd_adapters::register_all_adapters;
    use wtcd_core::types::ConfidenceBand;

    #[test]
    fn test_basic_exports_extraction() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/basic_exports.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert_eq!(result.confidence, ConfidenceBand::High);
        assert!(result.exports.len() >= 3, "Expected at least 3 exports, got {}", result.exports.len());

        let names: Vec<&str> = result.exports.iter().map(|e| e.name.as_str()).collect();
        assert!(names.contains(&"greet"), "Missing export: greet");
        assert!(names.contains(&"UserService"), "Missing export: UserService");
        assert!(names.contains(&"MAX_RETRIES"), "Missing export: MAX_RETRIES");
    }

    #[test]
    fn test_type_exports_extraction() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/type_exports.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert_eq!(result.confidence, ConfidenceBand::High);
        assert!(result.exports.len() >= 3, "Expected at least 3 exports, got {}", result.exports.len());

        let names: Vec<&str> = result.exports.iter().map(|e| e.name.as_str()).collect();
        assert!(names.contains(&"UserId"), "Missing export: UserId");
        assert!(names.contains(&"AppConfig"), "Missing export: AppConfig");
        assert!(names.contains(&"Status"), "Missing export: Status");
    }

    #[test]
    fn test_named_imports_extraction() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/named_imports.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert!(result.imports.len() >= 2, "Expected at least 2 imports, got {}", result.imports.len());
    }

    #[test]
    fn test_commonjs_require() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/commonjs.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert!(result.imports.len() >= 2, "Expected at least 2 require imports, got {}", result.imports.len());
        let sources: Vec<&str> = result.imports.iter().map(|i| i.source.as_str()).collect();
        assert!(sources.contains(&"fs"), "Missing require source: fs");
        assert!(sources.contains(&"path"), "Missing require source: path");
    }

    #[test]
    fn test_side_effects_detection() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/side_effects.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert!(result.side_effects.len() >= 3, "Expected at least 3 side effects, got {}", result.side_effects.len());

        let targets: Vec<&str> = result.side_effects.iter().map(|s| s.target.as_str()).collect();
        assert!(targets.iter().any(|t| t.contains("fs")), "Missing fs side effect");
        assert!(targets.iter().any(|t| t.contains("fetch")), "Missing fetch side effect");
        assert!(targets.iter().any(|t| t.contains("console")), "Missing console side effect");
    }

    #[test]
    fn test_broken_syntax_graceful() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/broken_syntax.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        // Should NOT crash, should have degraded confidence
        assert!(result.confidence == ConfidenceBand::Low || result.confidence == ConfidenceBand::None,
            "Expected Low or None confidence for broken file, got {:?}", result.confidence);
    }

    #[test]
    fn test_function_signatures() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/function_signatures.ts").unwrap();
        let result = adapter.parse(&source, "test.ts");

        assert!(result.signatures.len() >= 2, "Expected at least 2 signatures, got {}", result.signatures.len());

        let add_sig = result.signatures.iter().find(|s| s.name == "add").expect("Missing 'add' signature");
        assert_eq!(add_sig.parameters.len(), 2, "add should have 2 parameters");
        assert_eq!(add_sig.return_type, "number", "add should return number");
    }

    #[test]
    fn test_js_file_parsing() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.js").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/js/basic_exports.js").unwrap();
        let result = adapter.parse(&source, "test.js");

        assert_eq!(result.confidence, ConfidenceBand::High);
        assert!(result.exports.len() >= 3, "Expected at least 3 JS exports, got {}", result.exports.len());
    }

    #[test]
    fn test_json_output_serializable() {
        let registry = register_all_adapters().unwrap();
        let adapter = registry.find_adapter("test.ts").unwrap();

        let source = std::fs::read_to_string("tests/fixtures/ts/basic_exports.ts").unwrap();
        let file_result = adapter.parse(&source, "test.ts");

        let output = wtcd_core::types::RunOutput {
            api_version: "1".to_string(),
            files: vec![file_result],
            errors: vec![],
            summary: wtcd_core::types::RunSummary {
                total_files: 1,
                parsed_ok: 1,
                confidence_low: 0,
                confidence_none: 0,
                total_exports: 3,
                total_imports: 0,
                elapsed_ms: 0,
            },
        };

        let json = serde_json::to_string(&output).unwrap();
        let parsed: serde_json::Value = serde_json::from_str(&json).unwrap();

        assert_eq!(parsed["api_version"], "1");
        assert!(parsed["files"].is_array());
        assert!(parsed["summary"].is_object());
    }
    ```

    2. 创建 `tests/integration_init.rs`：
    ```rust
    use std::path::Path;

    #[test]
    fn test_config_template_valid_yaml() {
        // Verify the default config template is valid YAML
        let config = wtcd_core::config::Config::from_yaml(
            r#"version: 1
repo_root: .
scope:
  source_roots:
    - src
  exclude_patterns: []
mirror: {}
output:
  format: json
"#
        ).unwrap();

        assert_eq!(config.version, 1);
        assert_eq!(config.scope.source_roots, vec!["src"]);
    }
    ```
  </action>
  <verify>
    <automated>cd D:\dev\projects\anrsm_docs_v1 && cargo test 2>&1</automated>
  </verify>
  <done>
    All integration tests pass — exports, imports, signatures, side effects, broken syntax, JS, JSON serialization
  </done>
</task>

</tasks>

<verification>
- cargo test 全部通过
- Golden fixtures 验证所有提取类别
- broken_syntax 不崩溃
- JSON 输出结构正确
</verification>

<success_criteria>
- [ ] 8 个 golden fixture 文件创建
- [ ] integration_run.rs 包含 9 个测试
- [ ] integration_init.rs 包含 1 个配置模板测试
- [ ] cargo test 全部通过
- [ ] basic_exports.ts: 3+ exports, confidence=high
- [ ] broken_syntax.ts: confidence=low/none, 不崩溃
- [ ] side_effects.ts: 3+ side_effects (fs, fetch, console, localStorage)
- [ ] function_signatures.ts: 2+ signatures with typed params
- [ ] basic_exports.js: 3+ exports from JS file
- [ ] JSON 序列化/反序列化正确
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
